server:
  shutdown: graceful


# 애플리케이션 정보
spring:
  application:
    name: Muntum
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  # Server Shutdown
  lifecycle:
    timeout-per-shutdown-phase: 30s


# Observability
management:
  metrics:
    tags:
      application: Muntum
